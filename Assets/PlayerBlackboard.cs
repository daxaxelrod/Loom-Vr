using UnityEngine;
using System.Collections;

public class PlayerBlackboard : MonoBehaviour {

    // this is the class that moniters all global variables and 
    // any other information that needs to be passed from object to object
    public float euros;

	// Use this for initialization
	void Start () {
        euros = 0;
	}
	
	// Update is called once per frame
	void Update () {
	    
	}
}
